<%= form_for(client_phone) do |f| %>
  <% if client_phone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(client_phone.errors.count, "error") %> prohibited this client_phone from being saved:</h2>

      <ul>
      <% client_phone.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :client_id, 'Πελάτης' %>
    <%= f.select :client_id, options_for_select(Client.reverse_by_creation.map { |client| [client.name, client.id] }), {}, class: 'form-control' %>
  </div>

  <div class="field">
    <%= f.label :phone, 'Τηλέφωνο' %>
    <%= f.text_field :phone, class: 'form-control' %>
  </div>

  <div class="actions">
    <%= f.submit class: 'btn btn-success' %>
  </div>
<% end %>

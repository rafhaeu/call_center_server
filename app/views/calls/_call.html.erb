<tr>
  <td><%= call.phone %></td>
  <td>
    <%= 
      if call.client.present? 
        link_to "#{call.client.id} - #{call.client.name}", call.client 
      end %>
  </td>
  <td><%= format_datetime(call.called_at) %></td>
  <td><%= call.internal.name %></td>
  <td><%= call.duration %></td>
  <td><%= raw(calltype_icon(call.call_type_id)) %></td>
</tr>
